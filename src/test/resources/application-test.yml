spring:
  liquibase:
    enabled: true
    change-log: classpath:liquibase/changelog-master.yml
    default-schema: "carservicetest"
  datasource:
    username: postgres
    password: postgres
  jpa:
    properties:
      hibernate.show_sql: true
      hibernate:
        ddl-auto: validate
server:
  port: 8080